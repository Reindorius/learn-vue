```javascript
<!--
	计算属性：
	1. 定义： 要用的属性不存在，要通过已有的属性计算而来
    2. 远离： 通过Object.defineproperty的getter和setter
	3. get函数什么时候执行：
    	a. 初次读取时执行一次
		b. 依赖的数据发生变化的时候
	4. 为什么要用计算属性：
    	计算属性自带有缓存机制，和通过methods实现相比，可以复用，效率更高
    -->
```





